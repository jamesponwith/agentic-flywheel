# `go build ./tools/fleet/` drops a binary named `fleet` in the repo root.
# The roster moved to .flywheel/ so the collision with a config directory is
# gone (fw-oef.10); the stray binary is still worth ignoring.
/fleet

# stray blackbird daemon artifact, not ours
blackbird.db
