{
  "_caps_note": "concurrent_builders is now a CEILING, not a fixed width: Run sizes the fleet to the number of distinct repos with ready work, so it never spawns a builder there is nothing for. Two bounds hold it. Globally the ceiling, because every builder draws on ONE account quota \u2014 two of them emptied a session window in 29 minutes. And per repo exactly one, which is the bound that matters: blackbird keys reservations by project_key, so builders in DIFFERENT repos cannot collide, and serialising per repo keeps every builder solo in its own repo rather than re-arming the reservation requirement that cost three runs (fw-k8f). repos_per_night is the roster size, so a night can reach every repo that has work.",
  "_comment": "The fleet's constitution. Repo paths are relative to workspace_root ($FLYWHEEL_WORKSPACE, or ~/Workspace) so the roster is portable and says nothing about the host. Caps derive from review capacity, not compute (ADR 0006). Readable in ten seconds, diffable in review \u2014 keep it that way.",
  "workspace_root": "~/Workspace",
  "_runner": "How a builder is invoked (ADR 0010). The agent is a role, not a product \u2014 swap cmd/args for a different CLI, or set FLYWHEEL_AGENT_CMD to override everywhere including shell hooks. Args here REPLACE the built-in default rather than extending it, so a runner that omits its cost flag reports no spend and every run lands UNMEASURED \u2014 keep --output-format json, or whatever the swapped-in agent calls it.",
  "runner": {
    "cmd": "claude",
    "args": [
      "-p",
      "{prompt}",
      "--output-format",
      "json"
    ],
    "stdin": "none"
  },
  "caps": {
    "review_weight_per_night": 10,
    "concurrent_builders": 5,
    "repos_per_night": 5,
    "_quota": "The account is a Max subscription. It bills no marginal dollar, so the usd the runner reports is API-equivalent valuation and a measurement only \u2014 there is deliberately no dollar limit anywhere in this file. The real limit is a usage window, and the account states no remaining balance, so nothing here can pace against it in advance. What it does state is the moment the quota returns, in the 429 itself; the fleet records that and waits exactly that long (quotahold.go). window_hours is only how far back 'recent usage' is reported.",
    "quota": {
      "window_hours": 8
    }
  },
  "repos": [
    {
      "name": "llm-resiliency-router",
      "path": "llm-resiliency-router",
      "lang": "go"
    },
    {
      "name": "agentic-flywheel",
      "path": "agentic-flywheel",
      "lang": "go",
      "skip_stages": [
        "operate",
        "release"
      ]
    },
    {
      "name": "brax-tennis-rl",
      "path": "brax-tennis-rl",
      "lang": "python",
      "skip_stages": [
        "operate"
      ]
    },
    {
      "name": "flywheel-template",
      "path": "flywheel-template",
      "lang": "go",
      "paused": true,
      "role": "template"
    },
    {
      "name": "flywheel-template-py",
      "path": "flywheel-template-py",
      "lang": "python",
      "paused": true,
      "role": "template"
    }
  ],
  "agents": [
    {
      "name": "fleet/builder-go",
      "role": "builder",
      "repos": [
        "llm-resiliency-router",
        "agentic-flywheel",
        "flywheel-template"
      ],
      "skills": [
        "go",
        "shell"
      ]
    },
    {
      "name": "fleet/builder-py",
      "role": "builder",
      "repos": [
        "brax-tennis-rl"
      ],
      "skills": [
        "python",
        "jax",
        "rl"
      ]
    }
  ],
  "_agents_note": "Cut from 8 agents across 5 roles to 2 builders (retro 2026-08). Roster.Match had exactly one call site and only ever matched role=builder; coordinator, reviewer, operator and retro were declared and never dispatched. ADR 0001: a mechanism with zero call sites cannot improve by next month."
}
